# AdaRoboVLG anonymous project page

This release candidate is derived directly from the current debugging page. Its page styles, media orchestration, and interaction logic are unchanged except for code that exclusively served removed attribution and citation elements.

Local preview:

```powershell
node .\preview.mjs 8001
```

Then open `http://127.0.0.1:8001/`.

